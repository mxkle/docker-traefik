version: '3'

networks:
  web:
    external:
      name: dockertraefik_webgateway
      
services:
  whoami:
    image: jwilder/whoami
    networks:
      - web
    labels:
      - "traefik.backend=whoami"
      - "traefik.frontend.rule=Host:whoami.docker.localhost"
